"""Camera capture and hand-landmark tracking.

Kept independent of `canvas/` and `rendering/`: this subpackage only
turns webcam frames into structured hand-landmark data. Phase 2 adds
`smoothing.py`, `gestures.py`, `features.py`, and `calibration.py`
alongside `camera.py` and `tracker.py`.
"""
